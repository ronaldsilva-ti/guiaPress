<%- include ('./partials/header.ejs') %>
    <%- include ('./partials/homeNavbar.ejs') %>

        <body>
            <div class="container">
                <div class="jumbotron">
                    <h1 class="display-4">Bem Vindo!</h1>
                    <p class="lead">Isto é um simples blog</p>
                    <hr class="my-4">
                    <p>Isto é um simples blog criando por fins didaticos</p>
                    <a class="btn btn-primary btn-lg" href="#" role="button">Saber mais
                    </a>
                </div>

                <% articles.forEach(article => { %>
                    <div class="card-header">
                        <h1>
                            <%= article.title %>
                        </h1>

                    </div>
                    <div class="card-body">
                        <a href="/<%= article.slug %>" class="btn btn-success">Ler Artigo</a>

                    </div>
                    </br>
                    <%   }) %>
            </div>
        </body>
        <%- include ('./partials/footer.ejs') %>